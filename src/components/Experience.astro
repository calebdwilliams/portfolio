---
interface Props {
	title: string;
	company: string;
  endDate: string;
	shortDescription: string;
  startDate: string;
  href: string;
}

const { company, endDate, shortDescription, startDate, title, href } = Astro.props;
---

<li class="experience">
  <a href={href} target="_blank" rel="noopener noreferrer">
    <h3 class="experience__title">{title} • {company}</h3>
    <span class="experience__time">{startDate} - {endDate}</span>
    <p class="experience__description">{shortDescription}</p>
  </a>
</li>

<style>
  .experience {
    background: rgba(255, 255, 255, var(--background-opacity));
    border-color: transparent;
		box-sizing: border-box;
    display: grid;
    gap: var(--size-3);
    padding: var(--size-3);
  }
  .experience a {
    all: unset;
    cursor: pointer;
  }
  .experience:focus-within,
  .experience:hover {
    /* background: rgba(255, 255, 255, var(--background-opacity)); */
    box-shadow: 0 0 0 2px var(--white-0), 0 0 0 4px blue;
  }
  .experience__title {
    grid-area: title;

  }
  .experience__time {
    font-size: var(--font-size-0);
    grid-area: date;
    text-transform: uppercase;
  }
  .experience__description {
    grid-area: description;
  }
  .experience h3 {
    font-size: var(--font-size-1);
    font-weight: var(--font-weight-6);
  }
  .experience p {
    font-size: var(--font-size-1);
  }
</style>
